// Generated from policies/*.cedar. Run `pnpm policies:sync` after editing canonical policy files.
export const GENERATED_POLICY_TEXT: Readonly<Record<string, string>> = {
  "admins-export-pii": "@id(\"admins-export-pii\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"export_customer_data\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen { principal.hasTag(\"role\") && principal.getTag(\"role\") == \"admin\" };\n",
  "finance-approves-refunds": "@id(\"finance-approves-refunds\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"approve_refund\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"role\") &&\n  [\"finance\", \"admin\"].contains(principal.getTag(\"role\")) &&\n  context.input.amount <= 5000\n};\n",
  "finance-discounts-up-to-30": "@id(\"finance-discounts-up-to-30\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"apply_discount\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"role\") &&\n  (\n    principal.getTag(\"role\") == \"admin\" ||\n    (principal.getTag(\"role\") == \"finance\" && context.input.percent <= 30)\n  )\n};\n",
  "forbid-contractor-pii": "@id(\"forbid-contractor-pii\")\nforbid (\n  principal is Eve::User,\n  action == Eve::Action::\"export_customer_data\",\n  resource\n)\nwhen {\n  principal.hasTag(\"employment\") &&\n  principal.getTag(\"employment\") == \"contractor\"\n};\n",
  "forbid-customer-lookup-before-order-lookup": "@id(\"forbid-customer-lookup-before-order-lookup\")\nforbid (\n  principal is Eve::User,\n  action == Eve::Action::\"lookup_customer\",\n  resource\n)\nwhen { !(context.session.prior has lookup_order) };\n",
  "forbid-eu-staff-on-us-only-tools": "@id(\"forbid-eu-staff-on-us-only-tools\")\nforbid (\n  principal is Eve::User,\n  action == Eve::Action::\"cancel_order\",\n  resource\n)\nwhen { principal.hasTag(\"region\") && principal.getTag(\"region\") like \"eu-*\" };\n",
  "forbid-fraud-refunds": "@id(\"forbid-fraud-refunds\")\nforbid (\n  principal,\n  action == Eve::Action::\"process_refund\",\n  resource\n)\nwhen { context.input.reason like \"*fraud*\" || context.input.reason like \"*Fraud*\" };\n",
  "forbid-more-than-3-refunds-per-session": "@id(\"forbid-more-than-3-refunds-per-session\")\nforbid (\n  principal,\n  action == Eve::Action::\"process_refund\",\n  resource\n)\nwhen { context.session.counts.process_refund >= 3 };\n",
  "forbid-mutations-outside-business-hours": "@id(\"forbid-mutations-outside-business-hours\")\nforbid (\n  principal,\n  action in [Eve::Action::\"process_refund\", Eve::Action::\"cancel_order\"],\n  resource\n)\nwhen {\n  context.system.now.toTime() < duration(\"8h\") ||\n  context.system.now.toTime() > duration(\"20h\")\n};\n",
  "forbid-refund-budget-over-2000": "@id(\"forbid-refund-budget-over-2000\")\nforbid (\n  principal,\n  action == Eve::Action::\"process_refund\",\n  resource\n)\nwhen {\n  context.session.prior has process_refund &&\n  context.session.prior.process_refund.amountTotal + context.input.amount > 2000\n};\n",
  "leads-cancel-orders": "@id(\"leads-cancel-orders\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"cancel_order\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"role\") &&\n  [\"support-lead\", \"admin\"].contains(principal.getTag(\"role\"))\n};\n",
  "read-access-for-staff": "@id(\"read-access-for-staff\")\npermit (\n  principal is Eve::User,\n  action in [Eve::Action::\"lookup_order\", Eve::Action::\"lookup_customer\"],\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"scope\") &&\n  (principal.getTag(\"scope\") like \"*orders:read*\" || principal.getTag(\"scope\") like \"*orders:\\\\**\")\n};\n",
  "refund-large-requires-prior-approval": "@id(\"refund-large-requires-prior-approval\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"process_refund\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"role\") &&\n  [\"support-lead\", \"admin\"].contains(principal.getTag(\"role\")) &&\n  context.input.amount >= 500 &&\n  context.session has refundApproval &&\n  context.session.refundApproval.orderId == context.input.orderId &&\n  context.session.refundApproval.availableAmount >= context.input.amount &&\n  context.system.now.durationSince(context.session.refundApproval.latest) <= duration(\"1h\")\n};\n",
  "refund-under-500-for-leads": "@id(\"refund-under-500-for-leads\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"process_refund\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"role\") &&\n  [\"support-lead\", \"admin\"].contains(principal.getTag(\"role\")) &&\n  context.input.amount < 500\n};\n",
  "service-reconciler-readonly": "@id(\"service-reconciler-readonly\")\npermit (\n  principal == Eve::ServicePrincipal::\"svc-nightly-reconciler\",\n  action in [Eve::Action::\"lookup_order\", Eve::Action::\"lookup_customer\"],\n  resource == Eve::Agent::\"orderdesk\"\n);\n",
  "service-reconciler-summary-exports": "@id(\"service-reconciler-summary-exports\")\npermit (\n  principal == Eve::ServicePrincipal::\"svc-nightly-reconciler\",\n  action == Eve::Action::\"export_customer_data\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  context.input.format == \"summary\" &&\n  !(context.input has includePii && context.input.includePii == true)\n};\n",
  "shipping-changes-domestic-only": "@id(\"shipping-changes-domestic-only\")\npermit (\n  principal is Eve::User,\n  action == Eve::Action::\"update_shipping_address\",\n  resource == Eve::Agent::\"orderdesk\"\n)\nwhen {\n  principal.hasTag(\"role\") &&\n  (\n    principal.getTag(\"role\") == \"admin\" ||\n    (principal.getTag(\"role\") == \"support-lead\" && [\"US\", \"CA\"].contains(context.input.country))\n  )\n};\n",
};
